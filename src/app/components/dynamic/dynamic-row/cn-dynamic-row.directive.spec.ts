import {CnDynamicRowDirective} from './cn-dynamic-row.directive';

describe('CnDynamicRowDirective', () => {
  it('should create an instance', () => {
    const directive = new CnDynamicRowDirective();
    expect(directive).toBeTruthy();
  });
});
