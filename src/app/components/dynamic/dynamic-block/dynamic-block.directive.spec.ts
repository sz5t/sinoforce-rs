import {DynamicBlockDirective} from './dynamic-block.directive';

describe('DynamicBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicBlockDirective();
    expect(directive).toBeTruthy();
  });
});
